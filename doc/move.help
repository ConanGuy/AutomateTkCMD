move [@][id] <distance>:

Move the bloc you want to move by <distance> pixels.

    - [id]: id of the bloc you want to move. If blank,
            move the current selected bloc.
    - [@]: if refers, set the current selected bloc to
           the bloc asked.

Examples:
        move @bot 10
    Move the bloc 'bot' by 10 pixels and select it.

        move bot -3
    Move the bloc 'bot' by -3 pixels

        move 8
    Move the current selected bloc by 8 pixels